'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

var _color = require('color');

var _color2 = _interopRequireDefault(_color);

var _config = require('../config');

var _config2 = _interopRequireDefault(_config);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getForegroundColor(backgroundColor) {
  var colors = _config2.default.colors;


  if (arguments.length > 1) {
    for (var i = 1; i < arguments.length; i++) {
      console.log('arguments[' + i + ']', arguments[i]);
      if (_typeof(arguments[i]) === 'object' && arguments[i].backgroundColor) {
        backgroundColor = arguments[i].backgroundColor;
      }
    }
  }

  try {
    var color = (0, _color2.default)(backgroundColor).dark() ? colors.white : colors.black;
    console.log(color);
    return color;
  } catch (e) {
    return null;
  }
}

exports.default = getForegroundColor;